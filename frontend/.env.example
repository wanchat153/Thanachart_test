# คัดลอกไฟล์นี้เป็น .env.local แล้วแก้ค่าตามเครื่องของคุณ
# (.env.local ถูก ignore ไม่เข้า VCS — ห้ามใส่ secret ในไฟล์ .env.example นี้)

# base URL ของ backend ASP.NET Core (profile "http" ตาม launchSettings.json)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5126
